name: Build and Test Microservice

on:
  pull_request:

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      mail-service: ${{ steps.filter.outputs.mail-service }}
      message-queue: ${{ steps.filter.outputs.message-queue }}
      pet-service: ${{ steps.filter.outputs.pet-service }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Detect changed files
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            mail-service:
              - 'mail-service/**'
            message-queue:
              - 'message-queue/**'
            pet-service:
              - 'pet-service/**'

  mail-service:
    needs: detect-changes
    if: ${{ needs.detect-changes.outputs.mail-service == 'true' }}
    uses: ./.github/workflows/build-and-test.yml
    with:
      service: "mail-service"

  message-queue:
    needs: detect-changes
    if: ${{ needs.detect-changes.outputs.message-queue == 'true' }}
    uses: ./.github/workflows/build-and-test.yml
    with:
      service: "message-queue"

  pet-service:
    needs: detect-changes
    if: ${{ needs.detect-changes.outputs.pet-service == 'true' }}
    uses: ./.github/workflows/build-and-test.yml
    with:
      service: "pet-service"
